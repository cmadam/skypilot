# Reference ~/.sky/config.yaml for BlueVela LSF cluster
# Copy to ~/.sky/config.yaml on the machine running SkyPilot/gbserver
lsf:
  allowed_clusters:
    - bluevela
  cluster_configs:
    bluevela:
      workdir: /proj/granite-build/g4os/skypilot
      tmpdir: /opt/nvme/$USER/skypilot-tmp
      enroot:
        enabled: true
        share_path: /proj/granite-build/g4os
        use_local_nvme: true
        squash_options: "-comp lz4 -Xhc -no-xattrs"
      enroot_mounts:
        - "/dev/shm /dev/shm"
        - "/dev/infiniband /dev/infiniband"
      nccl_tuning_file: /proj/granite-build/g4os/bv-nccl-tuning.sh
      bsub_options:
        G: grp_granite_dot_build
        M: 64G
